.DS_Store
oracle-cdc-connect.config
clusterlink-onprem-source.config
schema-registry.config
client_ssl.properties.config
mongo-atlas-sink.json
rabbitmq
oracle-rds-copy
sami_oracle_id_rsa
ksqldb.sql
mongo-atlas-sink.json

# terraform things 
*.tfstate
*.tfstate.*
# Exclude all .tfvars files, which are likely to contain sentitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
*.tfvars

# Terraform's scratch dir
.terraform

# misc notes 
scratch.md
nothing.gitignore
README_gitig.md
TBD.md
copy_README_gitig.md.gitignore